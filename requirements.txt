rich
tqdm
termcolor
numpy
tokenizers>=0.19.1
torch>=2.3.0
transformers>=4.41.1
accelerate>=0.30.1
bitsandbytes>=0.43.1

# vLLM (optional, for high-performance inference)
# Uncomment the line below to install vLLM
vllm>=0.8.1